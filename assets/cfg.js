/* Public, client-only configuration. Never place repository tokens or secrets in browser JavaScript. */
window.IG_CONFIG = Object.freeze({
  repository: "www-infinity4/Image-Generator",
  clientOnly: true,
  externalWritesEnabled: false
});
